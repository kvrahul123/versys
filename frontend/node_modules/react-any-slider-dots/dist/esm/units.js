export var reactSlickAdapter = function (dots) {
    var dotsNodes = dots;
    var dotsCount = dotsNodes.length;
    var activeIndex = dotsNodes.findIndex(function (_a) {
        var props = _a.props;
        return props.className === 'slick-active';
    });
    return { dotsCount: dotsCount, activeIndex: activeIndex };
};
